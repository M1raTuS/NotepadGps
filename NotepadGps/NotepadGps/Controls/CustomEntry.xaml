<?xml version="1.0" encoding="UTF-8"?>
<Grid xmlns="http://xamarin.com/schemas/2014/forms" 
      xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
      xmlns:controls="clr-namespace:NotepadGps.Controls;assembly=NotepadGps"
      x:Class="NotepadGps.Controls.CustomEntry"
      x:Name="customEntry">

    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Label Grid.Row="0"
               FontSize="18"
               Text="{Binding LblText, Source={x:Reference customEntry}}"
               TextColor="#3D4E61"/>

        <Frame CornerRadius="2"
               Grid.Row="1"
               BackgroundColor="Transparent"
               BorderColor="Gray">

            <Grid ColumnDefinitions="*, 30">

                <controls:StandartEntry Grid.Column="0"
                                        Placeholder="{Binding Placeholder, Source={x:Reference customEntry}}"
                                        PlaceholderColor="{Binding PlaceholderColor, Source={x:Reference customEntry}}"
                                        Text="{Binding Text, Source={x:Reference customEntry}}" 
                                        FontSize="21"
                                        x:Name="cusEntry"/>

                <Image Grid.Column="1"
                       x:Name="img"
                       Source="ic_clear"
                       VerticalOptions="Fill"
                       HorizontalOptions="Fill">

                    <Image.GestureRecognizers>

                        <TapGestureRecognizer Command="{Binding TapGestureRecognizer,Source={x:Reference customEntry}}" />

                    </Image.GestureRecognizers>

                </Image>

            </Grid>

        </Frame>

        <Label Grid.Row="2"
               FontSize="15"
               Text="{Binding errorText, Source={x:Reference customEntry}}"
               IsVisible="{Binding isErrorVisible, Source={x:Reference customEntry}}"
               TextColor="#F24545"/>

    </Grid>

</Grid>